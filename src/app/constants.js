(function () {
    angular.module('photo-state.constants', [])

        .config([function () {
        }])

        .run(function run() {
        })

        .constant('appConfig', {
            inputMaxChars: 200
        });
})
();

